name: Cleanup Tencent TCR tags
description: Keep only the newest tags in a Tencent personal image repository.
runs:
  using: composite
  steps:
    - name: Cleanup old Tencent TCR tags
      shell: bash
      run: python3 "$GITHUB_WORKSPACE/scripts/ci/cleanup-tencent-tags.py"
